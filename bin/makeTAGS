#!/bin/bash
set -x -e

EMACSLISP=/usr/local/share/emacs/25.1/lisp

if which ctags-exuberant >& /dev/null
then

#tag=etags -
tag="ctags-exuberant -e -L -"

find $EMACSLISP -name "*.el" | $tag -f systemTAGS
find . -name save -prune -o -name pending -prune -o \( -name "*.el" -print \) | $tag

fi
